= form_for invite, url: invites_path do |f|
  = f.hidden_field :team_id, value: team_id
  = f.label :email, "Emails"
  = text_field_tag("emails[]", nil, placeholder: 'Enter an email', class: "invite-list")
  = f.submit 'Send Invite'
